// geminiService.ts - Safe Version

const API_KEY = "YOUR_API_KEY_HERE"; // We'll put the real key back later

export async function getGeminiResponse(userMessage: string) {
  // Simulating a response for now so the app doesn't crash
  console.log("User asked:", userMessage);
  
  return `I'm currently being updated to connect with the new RasuRide Database! 
  Please check back soon to chat about our live fleet of vehicles.`;
}